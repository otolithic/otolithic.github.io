html, body {
    background-color: #64C6DC;
    font-family: sans-serif;
    margin: 0;
    padding: 0;
    
    /*deletable? not sure what this does*/
    min-width: 100%;
}

/*
    index page only
*/
header {
    position: relative;
    text-align: center;
    height: 180px;
    margin: 20px;
}

nav {
    position: relative;
    left: 0px;
    height: 450px;
    text-align: center;

    background-color: azure;
    box-shadow: 0 0 20px black;
    
    /*border for browsers that don't support border-image*/
    border: 20px double;
    
    border-image:url(border1.png) 20 0 20 0 repeat;
    border-left: none;
    border-right: none;
}

nav ul {
    position: relative;
    border-top: 4px solid black;
    border-bottom: 4px solid black;
    
    width: 100%;
    height: 99%;
    margin: 0;
    padding: 0;
    
    /*sideways scrolling when there are too many links to fit*/
    overflow: auto;
    overflow-y: hidden;
    white-space: nowrap;
}

nav li {
    position: relative;
    display: inline-block;
    height: 100%;
    width: 300px;
    
    background-image: url("dancingman1.png");
    background-repeat: no-repeat;

    overflow: auto;
}

/*makes the men in the nav bar shake when hovered over*/
nav li:hover {
    -webkit-animation: shake 200ms infinite alternate;
    animation: shake 200ms infinite alternate;
}

nav a {
    position: relative;
    font-weight: bold;
    
    /*center the text on the men's chests and enlarge clickable area*/
    top: 150px;
    padding: 60px 60px 100px 60px;
    
    /*for glow on hover*/
    transition: text-shadow 0.5s linear;
}

nav a:hover {
    text-shadow: 0px 0px 5px cyan;
}

section {
    position: relative;
}

/*common to all pages*/
a {
    /*no underline*/
    text-decoration: none;
    color: black;
    
    /*sets up color change when hovered over*/
    transition: color 150ms linear;
}

a:hover {color: blueviolet;}

footer {
    background-color: #2aa1bb;
    box-shadow: 0 0 15px black inset;
    position: relative;
    text-align: center;
    margin-top: 75px;
    height: 300px;
}

footer p {
    padding-top: 20px;
}

/*
    other pages' styling & tricks
*/

#colorname {
	position: relative;
	text-align: center;
	font-size: 5em;
}

#colorbox {
    position: relative;
    left:50%-100px;
    background-color: rgb(0,0,0);
    border: 2px black dashed;
    width: 300px;
    height: 300px;
}

/*
    special
*/
/*shake animation for hovering over nav links*/
@-webkit-keyframes shake {
    0% {-webkit-transform:translateX(0px);}
    25% {-webkit-transform:translateX(5px); -webkit-transform:translateY(5px);}
    50% {-webkit-transform:translateY(-5px);}
    75% {-webkit-transform:translateX(-5px);}
    100% {-webkit-transform:translateX(0px);}
}
         
@keyframes shake {
    0% {transform:translateX(0px);}
    25% {transform:translateX(5px); transform:translateY(5px);}
    50% {transform:translateY(-5px);}
    75% {transform:translateX(-5px);}
    100% {transform:translateX(0px);}
 }